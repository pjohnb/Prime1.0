"""PRIME v1.0 machine-learning data layer.

Houses the Auto Execution Phase 1 training-data capture pipeline. The
prime_ml_dataset table (one row per APPROVED signal, outcome fields filled in
on trade close) is the labelled dataset future models train on.
"""
